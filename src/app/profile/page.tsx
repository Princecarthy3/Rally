"use client";

import { CheckCircle2, LoaderCircle, UserRound } from "lucide-react";
import { FormEvent, useEffect, useState } from "react";
import { ProtectedPage } from "@/components/protected-page";
import { useAuth } from "@/components/auth-provider";
import { getSupabaseBrowserClient } from "@/lib/supabase/client";

export default function ProfilePage(){
  const { profile,user,refreshProfile }=useAuth();
  const [name,setName]=useState(""); const [avatar,setAvatar]=useState(""); const [busy,setBusy]=useState(false); const [message,setMessage]=useState(""); const [error,setError]=useState("");
  useEffect(()=>{queueMicrotask(()=>{setName(profile?.display_name||user?.user_metadata?.display_name||"");setAvatar(profile?.avatar_url||"")})},[profile,user]);
  async function save(e:FormEvent){e.preventDefault();setMessage("");setError("");if(name.trim().length<2||name.trim().length>24){setError("Display name must be 2–24 characters.");return}const supabase=getSupabaseBrowserClient();if(!supabase||!user)return;setBusy(true);const {error:saveError}=await supabase.from("profiles").update({display_name:name.trim(),avatar_url:avatar.trim()||null,updated_at:new Date().toISOString()}).eq("id",user.id);if(saveError)setError(saveError.message);else{await refreshProfile();setMessage("Profile saved.")}setBusy(false)}
  const initial=name.trim().charAt(0).toUpperCase()||"R";
  return <ProtectedPage><main className="mx-auto max-w-4xl px-5 pb-28 pt-10 lg:px-8 lg:pt-14"><p className="text-xs font-extrabold uppercase tracking-[.16em] text-violet-600">Your player card</p><h1 className="mt-2 text-4xl font-black tracking-[-.05em]">Profile</h1><p className="mt-3 text-slate-500">Choose how friends will recognize you in Rally.</p><div className="mt-10 grid gap-6 md:grid-cols-[280px_1fr]"><aside className="rounded-[28px] bg-slate-950 p-7 text-center text-white"><div className="mx-auto grid h-28 w-28 place-items-center overflow-hidden rounded-full border-4 border-white/10 bg-violet-600 text-5xl font-black">{avatar?<img src={avatar} alt="Profile avatar" className="h-full w-full object-cover"/>:initial}</div><h2 className="mt-5 truncate text-xl font-black">{name||"Player"}</h2><p className="mt-1 text-sm text-slate-400">Rally player</p><div className="mt-7 grid grid-cols-2 gap-2 border-t border-white/10 pt-6"><div><strong className="block text-xl">{profile?.wins??0}</strong><span className="text-xs text-slate-500">Wins</span></div><div><strong className="block text-xl">{profile?.games_played??0}</strong><span className="text-xs text-slate-500">Games</span></div></div></aside><form onSubmit={save} className="rounded-[28px] border border-slate-200 bg-white p-6 md:p-8"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-violet-50 text-violet-600"><UserRound size={20}/></span><div><h2 className="font-black">Public details</h2><p className="text-xs text-slate-400">Visible to opponents you play.</p></div></div><div className="mt-7 space-y-5"><label className="block"><span className="mb-2 block text-sm font-bold">Display name</span><input value={name} onChange={e=>setName(e.target.value)} maxLength={24} required className="focus-ring w-full rounded-2xl border border-slate-200 px-4 py-3.5 outline-none"/><span className="mt-1.5 block text-right text-xs text-slate-400">{name.length}/24</span></label><label className="block"><span className="mb-2 block text-sm font-bold">Avatar image URL <span className="font-normal text-slate-400">(optional)</span></span><input value={avatar} onChange={e=>setAvatar(e.target.value)} type="url" placeholder="https://…" className="focus-ring w-full rounded-2xl border border-slate-200 px-4 py-3.5 outline-none"/></label><label className="block"><span className="mb-2 block text-sm font-bold">Account email</span><input value={user?.email||""} disabled className="w-full rounded-2xl border border-slate-100 bg-slate-50 px-4 py-3.5 text-slate-400"/></label>{error&&<p role="alert" className="rounded-xl bg-red-50 px-4 py-3 text-sm text-red-700">{error}</p>}{message&&<p role="status" className="flex items-center gap-2 rounded-xl bg-emerald-50 px-4 py-3 text-sm text-emerald-700"><CheckCircle2 size={16}/>{message}</p>}<button disabled={busy} className="flex w-full cursor-pointer items-center justify-center gap-2 rounded-2xl bg-violet-600 py-3.5 font-bold text-white disabled:opacity-60">{busy&&<LoaderCircle size={17} className="animate-spin"/>}Save profile</button></div></form></div></main></ProtectedPage>
}
